/**
 * เพิ่มเข้ามาพร้อมฟีเจอร์ "อนุมัติสมาชิกใหม่" รอบปรับปรุง (2026-08-10) — ผู้ใช้ (Ben) ระบุชัดเจนว่าอยากให้
 * เฉพาะบัญชีของตัวเองเท่านั้นที่เห็น/ใช้เมนูนี้ได้ ("จะมีแค่ฉันที่เห็นเมนูอนุมัติสมาชิกใหม่ ส่วนสมาชิกที่สมัคร
 * เพิ่มจะมองไม่เห็น เพื่อไม่ให้อนุมัติโดยที่ฉันไม่ได้อนุญาต") — ต่างจากทุกฟีเจอร์อื่นในระบบที่สมาชิกทุกคนใน
 * บริษัทเดียวกันมีสิทธิ์เท่ากันหมด (ไม่มี role) นี่คือจุดแรกที่มีสิทธิ์ระดับ "แอดมิน" แยกออกมาจริงๆ
 *
 * ระบุตัวตนด้วย user id ตรงๆ (ไม่ใช่ email) เพราะเสถียรกว่า — id ของ user ใน Supabase Auth ไม่มีทางเปลี่ยน
 * ต่างจาก email ที่แก้ไขได้ภายหลัง
 *
 * สำคัญ: ค่านี้ที่ฝั่ง client (ใช้ซ่อน/แสดงเมนูใน Sidebar.tsx และกันเข้าหน้า ManageMembersPage.tsx) เป็นแค่
 * ตัวกรอง UI เท่านั้น ไม่ใช่ชั้นความปลอดภัยจริง — ตัวบังคับสิทธิ์จริงอยู่ที่ RPC list_pending_users()/
 * approve_member() ในฐานข้อมูล (ดู supabase/migration_011_admin_only_approval.sql) ซึ่งเช็ค user id เดียวกัน
 * นี้อีกชั้นเสมอ ต่อให้มีคนแก้โค้ดฝั่ง client เพื่อบังคับแสดงเมนู/เรียก API เอง ก็ยังเรียก RPC ไม่ผ่านอยู่ดี
 */
export const PRIMARY_ADMIN_USER_ID = '6d608f39-0240-4088-b086-6d10b36ec478';

export function isPrimaryAdmin(userId: string | null | undefined): boolean {
  return userId === PRIMARY_ADMIN_USER_ID;
}
